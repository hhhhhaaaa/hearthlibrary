import React, { useState } from "react";
import Form from "react-bootstrap/Form";
import Button from "react-boostrap/Button";
import "Login.css"; //needs to be changed to our actual css file

function login() {
    retun (
        <div className="login" >
            <h1>Login Page</h1>
        </div>
    );
}

export  default login;